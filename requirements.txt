# CrusherFlow dependency/download manifest
#
# This project is a Node.js app, so `package.json` and `package-lock.json` are the
# authoritative install manifests. This file is a human-readable summary of what
# must be downloaded for local setup and deployment.

Node.js 18+
npm
Docker Desktop (for local Postgres via Docker Compose and image builds)

# Runtime npm packages (installed with `npm install`)
express@^4.18.2
cors@^2.8.5
pg@^8.11.0

# Local database for development
PostgreSQL 15 (via `docker compose up -d`)
